PORT=4100
PORT_STRICT=1
# CORS origins separados por coma
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Credenciales Postgres (usa DATABASE_URL si prefieres)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=labdb
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/labdb

# JWT
JWT_SECRET=dev_secret_cambia_esto
JWT_EXPIRES=7d

# Logs CORS opcional
# CORS_LOG=1
