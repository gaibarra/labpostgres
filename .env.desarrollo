## Backend + Frontend unified env (root)
PORT=4100
PORT_STRICT=1
NODE_ENV=development

## PostgreSQL (mirroring server/.env so root start funciona)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=6Vlgpcr&
PGDATABASE=lab
# DATABASE_URL=postgresql://postgres:6Vlgpcr&@localhost:5432/lab

## Auth
JWT_SECRET=una_clave_larga_muy_secreta_cambiala

## CORS
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:5174,http://127.0.0.1:5174
# CORS_LOG=0

## Frontend API base (usa proxy vite)
VITE_API_BASE_URL=/api

## AI
# La clave de OpenAI ahora se gestiona únicamente vía la configuración persistida (integraciones) en la base de datos.
# NO colocar aquí la OPENAI_API_KEY para evitar dependencias ocultas.

