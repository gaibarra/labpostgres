// Placeholder e2e test para AI Parameter.
// Este archivo estaba vacío causando que Vitest falle con "No test suite found".
// Mantener hasta implementar escenarios reales (generación parámetro vía /api/ai ...).
import { describe, it, expect } from 'vitest';

describe('AI Parameter (placeholder)', () => {
	it('placeholder passes', () => {
		expect(true).toBe(true);
	});
});

