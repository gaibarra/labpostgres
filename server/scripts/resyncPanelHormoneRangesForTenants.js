#!/usr/bin/env node
/**
 * Script archivado. Ya no realiza ninguna operación y se mantiene vacío
 * para conservar compatibilidad con referencias antiguas del repositorio.
 */
if (require.main === module) {
  console.log(JSON.stringify({ ok: true, message: 'script archivado' }));
}
module.exports = {};
